<table>
  <tr>
    <th><%= f.label :nombre%></th>
    <td><%= f.text_field :nombre%></td>
  </tr>
  <tr>
    <th><%= f.label :descripcion %></th>
    <td><%= f.text_field :descripcion %></td>
  </tr>
  <tr>
    <th><%= f.label :pais_id %></th>
    <td><%= f.select :pais_id, @paises.collect{|e| [e.nombre,e.id]}, {include_blank: "Selecciona uno."} %></td>
  </tr>
  <tr>
    <th></th>
    <td><%= f.submit "Guardar" %></td>
  </tr>
</table>
<%= link_to 'Regresar', estados_inicio_path %>
