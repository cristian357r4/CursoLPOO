<!DOCTYPE html>
<html>
  <head>
    <title>Proyectolpoo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="topnav">
      <a class="active" href="<%= paises_inicio_path %>">Paises</a>
      <a href="<%= estados_inicio_path %>">Estados</a>
      <a href="<%= usuarios_path %>">Usuarios</a>
    </div>
    <%= yield %>
  </body>
  <div class="blockquote-footer">
    <%= debug(params) if Rails.env.development? %>
  </div>
</html>
